{
  "name": "megumi-tags-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --env=\"\"",
    "dev:dev": "wrangler dev --env dev",
    "deploy": "wrangler deploy --env=\"\"",
    "deploy:dev": "wrangler deploy --env dev",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260619.0",
    "typescript": "^5.9.3",
    "wrangler": "^4.40.2"
  }
}
